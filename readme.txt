# TESTS

Tests are structured to be executed with pytest.

# CONFIG

Before running the first time, utility/create_config.py must be run manually.
config.py can then be updated manually or by updating create_config and running it again.