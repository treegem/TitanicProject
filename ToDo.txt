- Score on training data is much higher than on validation data --> remove overfitting

- Use cross validation? Learning set might not be large enough.


# Done

- Rescale/Normalize Data (sklearn has a tool for that)
Georg:  Used sklearn StandardScaler. Should however not affect DecisionTrees.
